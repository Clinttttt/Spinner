bin/
obj/
node_modules/
.env

/agents/
/docs/

# Scratch space: screenshots, metro logs, one-off exports.
/tmp/
/artifacts/

# Tooling caches that must never be committed.
.vs/
.angular/
.expo/
dist/

# Local dev server logs
*.out.log
*.err.log

# Firebase client config. Carries the project's Android API key and this repository
# is public, so EAS supplies it as a file secret at build time instead.
google-services.json
